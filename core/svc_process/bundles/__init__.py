"""Demo App Bundle scripts bundled with the svc-process image.

Real bundles are marketplace/first-party packages shipped with (or synced
into) each stage-runner's image; this package holds the one demo bundle
(`waddles.core.demo.echo`, seeded by migration 071) that proves the
ingest -> process -> action pipeline actually executes end to end.
"""

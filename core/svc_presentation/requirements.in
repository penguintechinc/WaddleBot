# svc-presentation direct dependencies.
#
# libs/flask_core is installed separately in the Dockerfile (`pip install
# libs/flask_core`, its own requirements.in is the source of truth for its
# transitive deps -- quart, pydal, psycopg2-binary, redis, posthog, etc.)
# -- listed here only what svc-presentation's own app code (app.py,
# config.py, blueprints/, services/) imports directly, matching
# hub_api/requirements.in's own header-comment convention.
quart>=0.23.0,<0.24.0
hypercorn>=0.18.0,<0.19.0
pydal>=20240906.1,<20260601
psycopg2-binary>=2.9.9,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# services/presentation_hub.py (Valkey pub/sub relay) and
# services/queue_reader.py (Valkey-backed music-queue read) both talk to
# Valkey via redis.asyncio -- redis-py is protocol-compatible with Valkey
# (this repo's already-deployed engine, k8s/helm/waddlebot/values.yaml's
# `infrastructure.redis.image: valkey/valkey:8-bookworm`) and is the
# established client this monorepo already uses against it elsewhere
# (core/credential_manager_module, core/ai_researcher_module,
# core/analytics_core_module, core/security_core_module all pin
# `redis>=5.0.0`) -- not a new, second Redis-protocol client library.
redis>=5.0.1,<6.0.0

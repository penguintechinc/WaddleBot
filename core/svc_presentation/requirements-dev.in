# svc-presentation dev/test-only dependencies -- never installed in the
# production image (Dockerfile installs requirements.txt only).
-r requirements.in
# 9.0.3 floor: PYSEC-2026-1845 (pip-audit flagged 8.4.2 -- test-security's
# pip-audit gate), matching general.md "fix alerts immediately" rule.
pytest>=9.0.3,<10.0.0
pytest-asyncio>=1.0.0,<2.0.0
pytest-cov>=5.0.0,<6.0.0
mypy>=1.13.0,<2.0.0
ruff>=0.7.0,<1.0.0
bandit>=1.7.10,<2.0.0

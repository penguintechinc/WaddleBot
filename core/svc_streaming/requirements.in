# svc-streaming -- Dependencies. Real control plane (Quart/pydal) + real
# ffmpeg subprocess data plane -- see app.py's module docstring.

# Core Framework
quart>=0.23.0,<0.24.0
quart-schema>=0.25.0,<0.26.0
hypercorn>=0.16.0

# HTTP client (token-ledger debit calls to hub-api, Twitch/YouTube live APIs)
httpx>=0.27.0,<0.28.0

# Environment
python-dotenv>=1.0.0

# Development / test
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

apiVersion: apps/v1
kind: Deployment
metadata:
  name: memories-interaction-module
  namespace: waddlebot
  labels:
    app: memories-interaction-module
    component: interaction-module
spec:
  replicas: 2
  selector:
    matchLabels:
      app: memories-interaction-module
  template:
    metadata:
      labels:
        app: memories-interaction-module
        component: interaction-module
    spec:
      containers:
      - name: memories-interaction-module
        image: waddlebot/memories-interaction-module:latest
        ports:
        - containerPort: 8031
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: waddlebot-db
              key: database-url
        - name: CORE_API_URL
          value: "http://router-service:8000"
        - name: ROUTER_API_URL
          value: "http://router-service:8000/router"
        - name: LABELS_API_URL
          value: "http://labels-core-service:8025"
        - name: MODULE_NAME
          value: "memories_interaction_module"
        - name: MODULE_VERSION
          value: "1.0.0"
        - name: MODULE_PORT
          value: "8031"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8031
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8031
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 2
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - name: tmp
          mountPath: /tmp
      volumes:
      - name: tmp
        emptyDir: {}
      restartPolicy: Always
      securityContext:
        fsGroup: 1000
---
apiVersion: v1
kind: Service
metadata:
  name: memories-interaction-service
  namespace: waddlebot
  labels:
    app: memories-interaction-module
spec:
  selector:
    app: memories-interaction-module
  ports:
  - port: 8031
    targetPort: 8031
    protocol: TCP
  type: ClusterIP
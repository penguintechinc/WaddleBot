---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: action-serverless
  labels:
    app: action-serverless
    app.kubernetes.io/component: action
spec:
  replicas: 2
  selector:
    matchLabels:
      app: action-serverless
  template:
    metadata:
      labels:
        app: action-serverless
        app.kubernetes.io/component: action
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      initContainers:
      - name: db-migrate
        image: waddlebot-migrations:latest
        imagePullPolicy: Always
        envFrom:
        - configMapRef:
            name: waddlebot-config
        - secretRef:
            name: waddlebot-secrets
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "200m"
            memory: "128Mi"
      containers:
      - name: action-serverless
        image: action-serverless:latest
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 8103
          protocol: TCP
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
        env:
        - name: MODULE_NAME
          value: "action-serverless"
        - name: MODULE_PORT
          value: "8103"
        envFrom:
        - configMapRef:
            name: waddlebot-config
        - secretRef:
            name: waddlebot-secrets
            optional: true
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
          limits:
            cpu: 1000m
            memory: 1Gi
        volumeMounts:
        - name: logs
          mountPath: /var/log/waddlebotlog
        - name: databases
          mountPath: /app/databases
      volumes:
      - name: logs
        emptyDir: {}
      - name: databases
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: action-serverless
  labels:
    app: action-serverless
    app.kubernetes.io/component: action
spec:
  type: ClusterIP
  selector:
    app: action-serverless
  ports:
  - name: http
    port: 8103
    targetPort: http
    protocol: TCP

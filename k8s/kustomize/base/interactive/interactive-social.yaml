---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: interactive-social
  labels:
    app: interactive-social
    app.kubernetes.io/component: interactive
spec:
  replicas: 2
  selector:
    matchLabels:
      app: interactive-social
  template:
    metadata:
      labels:
        app: interactive-social
        app.kubernetes.io/component: interactive
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      initContainers:
      - name: db-migrate
        image: waddlebot-migrations:latest
        imagePullPolicy: Always
        envFrom:
        - configMapRef:
            name: waddlebot-config
        - secretRef:
            name: waddlebot-secrets
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "200m"
            memory: "128Mi"
      containers:
      - name: interactive-social
        image: interactive-social:latest
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 8010
          protocol: TCP
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
        envFrom:
        - configMapRef:
            name: waddlebot-config
        - secretRef:
            name: waddlebot-secrets
            optional: true
        env:
        - name: MODULE_NAME
          value: "interactive-social"
        - name: MODULE_PORT
          value: "8010"
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
          limits:
            cpu: 1000m
            memory: 1Gi
        volumeMounts:
        - name: logs
          mountPath: /var/log/waddlebotlog
        - name: databases
          mountPath: /app/databases
      volumes:
      - name: logs
        emptyDir: {}
      - name: databases
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: interactive-social
  labels:
    app: interactive-social
    app.kubernetes.io/component: interactive
spec:
  type: ClusterIP
  selector:
    app: interactive-social
  ports:
  - name: http
    port: 8010
    targetPort: http
    protocol: TCP

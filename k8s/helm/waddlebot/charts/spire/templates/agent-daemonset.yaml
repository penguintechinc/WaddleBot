{{- if .Values.enabled }}
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: {{ include "waddlebot-spire.fullname" . }}-agent
  labels:
    {{- include "waddlebot-spire.labels" . | nindent 4 }}
    component: agent
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    matchLabels:
      {{- include "waddlebot-spire.selectorLabels" . | nindent 6 }}
      component: agent
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
  template:
    metadata:
      labels:
        {{- include "waddlebot-spire.selectorLabels" . | nindent 8 }}
        component: agent
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "waddlebot-spire.agentServiceAccountName" . }}
      hostPID: false
      securityContext:
        {{- toYaml .Values.agent.podSecurityContext | nindent 8 }}
      initContainers:
      - name: init-server-bundle
        image: busybox:1.36@sha256:73aaf090f3d85aa34ee199857f03fa3a95c8ede2ffd4cc2cdb5b94e566b11662
        imagePullPolicy: IfNotPresent
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop: [ALL]
        resources:
          requests:
            cpu: 25m
            memory: 32Mi
          limits:
            cpu: 100m
            memory: 64Mi
        command:
          - sh
          - -c
          - |
            until wget -qO- http://{{ include "waddlebot-spire.fullname" . }}-server:8080/ready 2>/dev/null; do
              echo "waiting for spire-server..."
              sleep 2
            done
      containers:
      - name: spire-agent
        # repository already carries @sha256:<digest> (see values.yaml) — do
        # not append :tag, that produces an invalid "name@sha256:x:tag" ref.
        image: "{{ .Values.agent.image.repository }}"
        imagePullPolicy: {{ .Values.agent.image.pullPolicy }}
        # ROOT EXCEPTION (approved): see values.yaml agent.podSecurityContext
        # comment — the agent must chown/create the hostPath Workload API
        # socket directory shared with every workload pod on the node.
        securityContext:
          {{- toYaml .Values.agent.securityContext | nindent 12 }}
        args:
          - -config
          - /run/spire/config/agent.hcl
        {{- with .Values.agent.resources }}
        resources:
          {{- toYaml . | nindent 12 }}
        {{- end }}
        livenessProbe:
          httpGet:
            path: /live
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
        volumeMounts:
        - name: spire-config
          mountPath: /run/spire/config
          readOnly: true
        - name: spire-agent-socket
          mountPath: {{ .Values.agent.socketDir }}
        - name: spire-data
          mountPath: /run/spire/agent
        - name: spire-bundle
          mountPath: /run/spire/bundle
        {{- if .Values.server.nodeAttestation.k8sPsat.enabled }}
        - name: spire-token
          mountPath: {{ dir .Values.agent.serviceAccountTokenPath }}
          readOnly: true
        {{- end }}
      volumes:
      - name: spire-config
        configMap:
          name: {{ include "waddlebot-spire.fullname" . }}-agent
      - name: spire-agent-socket
        hostPath:
          path: {{ .Values.agent.socketDir }}
          type: DirectoryOrCreate
      - name: spire-data
        emptyDir: {}
      - name: spire-bundle
        emptyDir: {}
      {{- if .Values.server.nodeAttestation.k8sPsat.enabled }}
      - name: spire-token
        projected:
          sources:
          - serviceAccountToken:
              path: {{ base .Values.agent.serviceAccountTokenPath }}
              audience: {{ .Values.agent.serviceAccountTokenAudience | quote }}
              expirationSeconds: {{ .Values.agent.serviceAccountTokenExpirationSeconds }}
      {{- end }}
      {{- with .Values.agent.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.agent.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}

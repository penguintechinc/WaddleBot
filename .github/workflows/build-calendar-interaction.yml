name: Build calendar-interaction

on:
  push:
    branches: [main, 'v*', 'release/**', 'feature/**', 'fix/**', 'chore/**', 'hotfix/**', 'docs/**', 'refactor/**']
    paths:
      - 'action/interactive/calendar_interaction_module/**'
      - 'libs/flask_core/**'
      - '.version'
      - '.github/workflows/build-calendar-interaction.yml'
      - '.github/workflows/build-container.yml'
  pull_request:
    branches: [main, 'v*', 'release/**', 'feature/**', 'fix/**', 'chore/**', 'hotfix/**', 'docs/**', 'refactor/**']
    paths:
      - 'action/interactive/calendar_interaction_module/**'
      - 'libs/flask_core/**'
  workflow_dispatch:


permissions:
  contents: read
  packages: write

jobs:
  build:
    name: Build calendar-interaction Container
    uses: ./.github/workflows/build-container.yml
    with:
      module_path: 'action/interactive/calendar_interaction_module'
      module_name: 'calendar-interaction'
      module_port: '8009'
    secrets: inherit

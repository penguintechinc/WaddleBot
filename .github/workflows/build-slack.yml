name: Build slack

on:
  push:
    branches: [main, 'v*', 'release/**', 'feature/**', 'fix/**', 'chore/**', 'hotfix/**', 'docs/**', 'refactor/**']
    paths:
      - 'trigger/receiver/slack_module/**'
      - 'libs/flask_core/**'
      - '.version'
      - '.github/workflows/build-slack.yml'
      - '.github/workflows/build-container.yml'
  pull_request:
    branches: [main, 'v*', 'release/**', 'feature/**', 'fix/**', 'chore/**', 'hotfix/**', 'docs/**', 'refactor/**']
    paths:
      - 'trigger/receiver/slack_module/**'
      - 'libs/flask_core/**'
  workflow_dispatch:


permissions:
  contents: read
  packages: write

jobs:
  build:
    name: Build slack Container
    uses: ./.github/workflows/build-container.yml
    with:
      module_path: 'trigger/receiver/slack_module'
      module_name: 'slack'
      module_port: '8012'
    secrets: inherit

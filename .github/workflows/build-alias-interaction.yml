name: Build alias-interaction

on:
  push:
    branches: [main, 'v*', 'release/**', 'feature/**', 'fix/**', 'chore/**', 'hotfix/**', 'docs/**', 'refactor/**']
    paths:
      - 'action/interactive/alias_interaction_module/**'
      - 'libs/flask_core/**'
      - '.version'
      - '.github/workflows/build-alias-interaction.yml'
      - '.github/workflows/build-container.yml'
  pull_request:
    branches: [main, 'v*', 'release/**', 'feature/**', 'fix/**', 'chore/**', 'hotfix/**', 'docs/**', 'refactor/**']
    paths:
      - 'action/interactive/alias_interaction_module/**'
      - 'libs/flask_core/**'
  workflow_dispatch:


permissions:
  contents: read
  packages: write

jobs:
  build:
    name: Build alias-interaction Container
    uses: ./.github/workflows/build-container.yml
    with:
      module_path: 'action/interactive/alias_interaction_module'
      module_name: 'alias-interaction'
      module_port: '8006'
    secrets: inherit
